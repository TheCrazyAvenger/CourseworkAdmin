import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 32,
  },
  input: {
    marginBottom: 16,
  },
  button: {
    marginTop: 38,
  },
  error: {
    marginTop: -16,
  },
});
